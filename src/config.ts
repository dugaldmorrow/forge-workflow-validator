
// This file contains the configuration for the application.

export const featureProjectKey = 'FEAT';
export const featureTestProjectKey = 'FT';

// This is a the ID of a rich text custom field that the app will update with a message providing compliance details. This 
// field needs to be on the issue screen(s) for users to see it. The field is updated using the Forge app's user account 
// which means that regular users will see it automatically refresh.
export const complianceFieldId = 'customfield_10058';

export const inProgressWorkflowStateId = '3';
export const resolvedWorkflowStateId = '5';
